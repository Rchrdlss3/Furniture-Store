const mongoose = require('mongoose');
const Schema = mongoose.Schema

const RequestPickUpSchema = new Schema({
    /*
    orderID:
    pick up: utc
    drop off: utc
    */
})

module.exports = mongoose.model()